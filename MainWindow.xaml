<Window x:Class="CyberSecurityChatbotGUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Cyber Security Chatbot"
        Height="600"
        Width="900"
        Background="#0D1B2A">

    <Grid Margin="10">

        <Grid.RowDefinitions>
            <RowDefinition Height="120"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="70"/>
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <Border Grid.Row="0"
                Background="#1B263B"
                CornerRadius="15"
                Padding="10"
                Margin="0,0,0,10">

            <StackPanel>

                <TextBlock Text="CYBER SECURITY ASSISTANT"
                           FontSize="28"
                           FontWeight="Bold"
                           Foreground="#00FFFF"
                           HorizontalAlignment="Center"/>

                <TextBlock Text="Protecting Your Digital World"
                           FontSize="16"
                           Foreground="White"
                           HorizontalAlignment="Center"
                           Margin="0,5,0,0"/>

            </StackPanel>

        </Border>

        <!-- CHAT DISPLAY -->
        <Border Grid.Row="1"
                Background="#1B263B"
                CornerRadius="15"
                Padding="10"
                Margin="0,0,0,10">

            <ScrollViewer VerticalScrollBarVisibility="Auto">

                <TextBlock x:Name="ChatHistory"
                           TextWrapping="Wrap"
                           Foreground="White"
                           FontSize="16"/>

            </ScrollViewer>

        </Border>

        <!-- INPUT AREA -->
        <Grid Grid.Row="2">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="150"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="UserInputBox"
                     Grid.Column="0"
                     FontSize="16"
                     Margin="0,0,10,0"
                     Padding="10"
                     Background="#415A77"
                     Foreground="White"
                     BorderBrush="#00FFFF"/>

            <Button x:Name="SendButton"
                    Grid.Column="1"
                    Content="SEND"
                    FontSize="18"
                    FontWeight="Bold"
                    Background="#00FFFF"
                    Foreground="Black"/>

        </Grid>

    </Grid>

</Window>